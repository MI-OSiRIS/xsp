-- BUILD INSTRUCTIONS --

Dependencies:
	-- libconfig-dev
	-- libtool
	-- gcc
	-- make
	-- automake
	-- autoconf

> ./autogen.sh
> ./configure #to create both shared n static libs
> ./configure --disable-shared #to create just static libs
> make
